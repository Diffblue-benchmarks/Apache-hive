# Copyright 2019 Diffblue Limited. All Rights Reserved.
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
  # Constrain the size of arrays created in the analyzed code.
  java-max-vla-length: 100000
phases:
  -
    # Phase 0 PRETTY, SIMPLE and SMART - pretty tests, do not test private methods directly, Smart-Harness
    timeout: 180
    cbmcArguments:
      # Use simple models first containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Instantiate classes using the simplest constructor strategy
      smart-harness: simplest-constructor-and-nondet
  -
    # Phase 1 PRETTY AND COMPLEX - standard models only, pretty tests, do not test private methods directly, input factory
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
      # Limit input factory recursive depth (default 0 = no recursion allowed)
      java-test-input-factory-bmc-recursion-limit: 4
  -
    # Phase 2 FIFO Strategy - pretty tests, do not test private methods directly, fifo
    timeout: 100
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Add constraint that strings are printable
      max-nondet-string-length: 100
      # Explores the program tree breadth-first.
      paths: fifo
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: false
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
    # Phase 3 Exceptions - do not test private methods directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
    # Phase 4 Direct testing - test private methods directly, increased reflection
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
    # PHASE 5 Mocking - aggressive mocking, do not test private methods directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Only load the class (and related inner/outer classes) containing the method under test,
      load-containing-class-only: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
# Copyright 2019 Diffblue Limited. All Rights Reserved.
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
  # Constrain the size of arrays created in the analyzed code.
  java-max-vla-length: 100000
phases:
  -
    # Phase 0 PRETTY, SIMPLE and SMART - pretty tests, do not test private methods directly, Smart-Harness
    timeout: 180
    cbmcArguments:
      # Use simple models first containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Instantiate classes using the simplest constructor strategy
      smart-harness: simplest-constructor-and-nondet
  -
    # Phase 1 PRETTY AND COMPLEX - standard models only, pretty tests, do not test private methods directly, input factory
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
      # Limit input factory recursive depth (default 0 = no recursion allowed)
      java-test-input-factory-bmc-recursion-limit: 4
  -
    # Phase 2 FIFO Strategy - pretty tests, do not test private methods directly, fifo
    timeout: 100
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Add constraint that strings are printable
      max-nondet-string-length: 100
      # Explores the program tree breadth-first.
      paths: fifo
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: false
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
    # Phase 3 Exceptions - do not test private methods directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
    # Phase 4 Direct testing - test private methods directly, increased reflection
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
    # PHASE 5 Mocking - aggressive mocking, do not test private methods directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Only load the class (and related inner/outer classes) containing the method under test,
      load-containing-class-only: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
units:
 - target:
   - dir: storage-api/target/classes
     file: org/apache/hive/common/util/Murmur3.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hive/common/util/Murmur3.java
 - target:
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/ql/exec/vector/expressions/StringExpr.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/ql/exec/vector/expressions/StringExpr.java
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/ql/exec/vector/BytesColumnVector.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/ql/exec/vector/BytesColumnVector.java
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/ql/exec/vector/expressions/StringExpr$BoyerMooreHorspool.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/ql/exec/vector/expressions/StringExpr.java
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/ql/exec/vector/expressions/StringExpr$Finder.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/ql/exec/vector/expressions/StringExpr.java
 - target:
   - dir: standalone-metastore/metastore-common/target/classes
     file: org/apache/hadoop/hive/metastore/utils/FileUtils.class
     sourceDir: standalone-metastore/metastore-common/src/main/java
     sourceFile: org/apache/hadoop/hive/metastore/utils/FileUtils.java
   - dir: standalone-metastore/metastore-common/target/classes
     file: org/apache/hadoop/hive/metastore/utils/HdfsUtils$1.class
     sourceDir: standalone-metastore/metastore-common/src/main/java
     sourceFile: org/apache/hadoop/hive/metastore/utils/HdfsUtils.java
   - dir: standalone-metastore/metastore-common/target/classes
     file: org/apache/hadoop/hive/metastore/utils/FileUtils$2.class
     sourceDir: standalone-metastore/metastore-common/src/main/java
     sourceFile: org/apache/hadoop/hive/metastore/utils/FileUtils.java
   - dir: standalone-metastore/metastore-common/target/classes
     file: org/apache/hadoop/hive/metastore/utils/FileUtils$3.class
     sourceDir: standalone-metastore/metastore-common/src/main/java
     sourceFile: org/apache/hadoop/hive/metastore/utils/FileUtils.java
   - dir: standalone-metastore/metastore-common/target/classes
     file: org/apache/hadoop/hive/metastore/conf/MetastoreConf$ConfVars.class
     sourceDir: standalone-metastore/metastore-common/src/main/java
     sourceFile: org/apache/hadoop/hive/metastore/conf/MetastoreConf.java
   - dir: standalone-metastore/metastore-common/target/classes
     file: org/apache/hadoop/hive/metastore/api/MetaException.class
     sourceDir: standalone-metastore/metastore-common/src/gen/thrift/gen-javabean
     sourceFile: org/apache/hadoop/hive/metastore/api/MetaException.java
   - dir: standalone-metastore/metastore-common/target/classes
     file: org/apache/hadoop/hive/metastore/utils/HdfsUtils.class
     sourceDir: standalone-metastore/metastore-common/src/main/java
     sourceFile: org/apache/hadoop/hive/metastore/utils/HdfsUtils.java
   - dir: standalone-metastore/metastore-common/target/classes
     file: org/apache/hadoop/hive/metastore/conf/MetastoreConf.class
     sourceDir: standalone-metastore/metastore-common/src/main/java
     sourceFile: org/apache/hadoop/hive/metastore/conf/MetastoreConf.java
   - dir: standalone-metastore/metastore-common/target/classes
     file: org/apache/hadoop/hive/metastore/utils/FileUtils$1.class
     sourceDir: standalone-metastore/metastore-common/src/main/java
     sourceFile: org/apache/hadoop/hive/metastore/utils/FileUtils.java
 - target:
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/common/TableName.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/common/TableName.java
 - target:
   - dir: vector-code-gen/target/classes
     file: org/apache/hadoop/hive/tools/GenVectorCode.class
     sourceDir: vector-code-gen/src
     sourceFile: org/apache/hadoop/hive/tools/GenVectorCode.java
   - dir: vector-code-gen/target/classes
     file: org/apache/hadoop/hive/tools/GenVectorTestCode.class
     sourceDir: vector-code-gen/src
     sourceFile: org/apache/hadoop/hive/tools/GenVectorTestCode.java
   - dir: vector-code-gen/target/classes
     file: org/apache/hadoop/hive/tools/GenVectorTestCode$TestSuiteClassName.class
     sourceDir: vector-code-gen/src
     sourceFile: org/apache/hadoop/hive/tools/GenVectorTestCode.java
   - dir: vector-code-gen/target/classes
     file: org/apache/hadoop/hive/tools/GenVectorCode$IfDefinedMode.class
     sourceDir: vector-code-gen/src
     sourceFile: org/apache/hadoop/hive/tools/GenVectorCode.java
 - target:
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/ql/util/TimestampUtils.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/ql/util/TimestampUtils.java
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/common/type/HiveDecimalV1.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/common/type/HiveDecimalV1.java
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/serde2/io/HiveDecimalWritable.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/serde2/io/HiveDecimalWritable.java
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/common/type/HiveDecimal.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/common/type/HiveDecimal.java
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/common/type/FastHiveDecimalImpl.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/common/type/FastHiveDecimalImpl.java
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/common/type/FastHiveDecimal.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/common/type/FastHiveDecimal.java
 - target:
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/common/type/FastHiveDecimalImpl.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/common/type/FastHiveDecimalImpl.java
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/common/type/FastHiveDecimal$FastCheckPrecisionScaleStatus.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/common/type/FastHiveDecimal.java
   - dir: storage-api/target/classes
     file: org/apache/hadoop/hive/common/type/FastHiveDecimal.class
     sourceDir: storage-api/src/java
     sourceFile: org/apache/hadoop/hive/common/type/FastHiveDecimal.java
